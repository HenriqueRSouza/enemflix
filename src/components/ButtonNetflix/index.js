import styled from "styled-components";

const ButtonNet = styled.button`
  color: var(--white);
  cursor: pointer;
  padding: 16px 24px;
  font-style: normal;
  font-weight: bold;
  font-size: 16px;
  outline: none;
  text-decoration: none;
  display: inline-block;
`;

export default ButtonNet;
